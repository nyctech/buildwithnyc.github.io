$( document ).ready(() => {
  if ($('div#nyc-forward').children().length > 2 ) {
    $('li.empty-message').hide()
  }
})

$( document ).ready(() => {
  if ($('div#nyc-x').children().length > 2 ) {
    $('li.empty-message').hide()
  }
})

$( document ).ready(() => {
  if ($('div#nyc-digital').children().length > 2 ) {
    $('li.empty-message').hide()
    console.log("Hello!!")
  }
})

$( document ).ready(() => {
  if ($('div#nyc-connected').children().length > 2 ) {
    $('li.empty-message').hide()
  }
})
